##from tkinter import *
##w, h = 745, 374
##win = Tk()
##canvas = Canvas(win, width = w, height = h)
##img = PhotoImage(file = "court.png")
##canvas.create_image(w/2, h/2, image = img)
##canvas.pack()
##win.mainloop()


##from tkinter import *
##win = Tk()
##x1, y1 = 10, 10
##x2, y2 = x1 + 30, y1 + 30
##x_dist, y_dist = 1, 1

##canvas = Canvas(win, width = 200, height = 200, bg = "white")
##ball = canvas.create_oval(x1, y1, x2, y2, fill = "yellow")

##def flow() :
    ##global x1,y1,x2,y2
    ##x1 += x_dist
    ##y1 += y_dist
    ##x2, y2 = x1 + 30, y1 + 30
    ##canvas.coords(ball, x1, y1, x2, y2)
    ##win.after(10, flow)

##flow()

##canvas.pack()
##win.mainloop()


##from tkinter import *

##class Court :

    ##def __init__(self, window, width, height, img) :
        
        ##self.canvas = Canvas(window, width = width, height = height)
        
        ##self.img = PotoImage(file = img)
        
        ##self.canvas.create_imge(width/2, height/2, image = self.img)
        ##self.width = width
        ##slef.height = height

        ##self.canvas.pack()

##from tkinter import *
##win = Tk()
##x1, y1 = 10, 10
##x2,y2 = x1 + 30, y1 + 30
##x_dist, y_dist = 1, 1

##canvas = Canvas(win, width = 200, height = 200, bg = "white")
##ball = canvas.create_oval(x1,y1,x2,y2, fill = "yellow")

##def flow() :
    ##global x1, y1, x2, y2
    ##x1 += x_dist
    ##y1 += y_dist
    ##x2, y2 = x1 + 30, y1 + 30
    ##canvas.coords(ball,x1,y1,x2,y2)
    ##win.after(10, flow)

##flow()

##canvas.pack()
##win.mainloop()


##from tkinter import *

##class Court :

    ##def __init__(self,window, width , height, img) :

        ##self.canvas = Canvas(window, width = width, height = height)

        ##self.img = PhotoImage(file = img)
        ##self.canvas.create_iamge(width/2, height/2, image = self.img)

        ##self.width = width
        ##self.height = height

        ##self.canvas.pack()


##from tkinter import *

##class Ball :
    ##def __init__ (self, court, x1, y1, x2, y2) :
        ##self.x1 = x2
        ##self.y1 = y1
       ## self.x2 = x2
        ##self.y2 = y2

        ##self.x_dist = 10
        ##self.y_dist = 10

        ##self.width = x2 - x1
        ##self.height = y2 - y1

        ##self.court = court
       ## self.canvas = court.canvas

        ##self.ball
